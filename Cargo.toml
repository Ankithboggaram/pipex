[package]
name = "pipex"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A high-performance, zero-allocation pipeline execution library for Rust."
license = "MIT OR Apache-2.0"
repository = "https://github.com/Ankithboggaram/pipex"
readme = "README.md"

[dependencies]
tracing = "0.1"

[dev-dependencies]
divan = "0.1"

[[bench]]
name = "pipeline_benchmarks"
harness = false
